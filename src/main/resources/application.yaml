server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: portfolio-backend
  data:
    mongodb:
      uri: ${MONGO_URI}

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}